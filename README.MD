# Plugin which added MySql driver to Velocity
## Build guide
- Clone the repo: `git clone https://github.com/SimpMC-Network/Velocity-MysqlDriverFix`
- Build: `./gradlew build`
Jar file is at `build/libs`
